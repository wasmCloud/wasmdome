# This file provides the same runtime host as the kvcounter.rs sample, but in manifest form
# To see this in action, `cargo run --example kvcounter_manifest --all-features` from the
# wascc-host root directory
---
actors:
    - /home/kevin/Code/wascc/wasmdome/NPC/turret/target/wasm32-unknown-unknown/release/turret_signed.wasm
bindings:
    - actor: "MCPBZXJDXCWRJAOPHXCBGVU55BAKCQSNUXUQRKRLI6RWYRFJW7W64JH4"
      capability: "wasmdome:engine"
      values:
capabilities: []
