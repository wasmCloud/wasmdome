[package]
name = "{{project-name}}"
version = "0.0.1"
authors = ["{{authors}}"]
edition = "2018"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmdome-mech-sdk = "0.0.1"

[profile.release]
# Optimize for small code size
opt-level = "s"
